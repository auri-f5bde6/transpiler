[workspace]
resolver = "3"
members = ["compiler", "lexer", "parser", "repl", "transpiler-wasm", "type-checker"]

[workspace.dependencies]
lexer = { path = "lexer" }
parser = { path = "parser" }
compiler = { path = "compiler" }
type-checker = { path = "type-checker" }
wasm-bindgen = "0.2.128"